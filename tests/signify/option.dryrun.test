#!/bin/sh
source ${0%/*}/rigging.sh || exit 70
# the --dryrun option enters dry-run mode, but we need to dry-run this (sic)
${exe?} --dryrun -y --private=/dev/null.private --public=/dev/null.public --request=/dev/null.request
test 0 == $?
