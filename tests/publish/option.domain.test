#!/bin/sh
source ${0%/*}/rigging.sh || exit 70
${exe?} -y --domain=something.something.dark-side.example.  -k /dev/null -n localhost.localdomain --doc=/dev/null.json
# enters warning mode, but we need to dry-run this
test 0 == $?
